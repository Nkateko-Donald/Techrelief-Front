import React from "react";
import SettingsClient from "./SettingsClient";

export const metadata = {
  title: "Profile – Siza Admin Settings",
};

export default function Page() {
  return <SettingsClient />;
}
